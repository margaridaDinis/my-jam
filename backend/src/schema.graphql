# import * from './generated/prisma.graphql'

type Mutation {
  createAlbum(
    year: Int,
    name: String,
    description: String,
    image: String,
    largeImage: String
  ): Album!
}

type Query {
  albums: [Album]!
}
